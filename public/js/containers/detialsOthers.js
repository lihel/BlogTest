/**
 * Created by lmy on 18-1-24.
 */
import {connect} from 'react-redux';
import detialsOthers from '../components/detialsOthers';

const mapStateToProps = (state) => {
    return {}
};

function mapDispatchToProps(dispatch) {
    return {}
}
export default connect(mapStateToProps, mapDispatchToProps)(detialsOthers);